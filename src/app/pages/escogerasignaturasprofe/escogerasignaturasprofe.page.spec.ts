import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EscogerasignaturasprofePage } from './escogerasignaturasprofe.page';

describe('EscogerasignaturasprofePage', () => {
  let component: EscogerasignaturasprofePage;
  let fixture: ComponentFixture<EscogerasignaturasprofePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(EscogerasignaturasprofePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
