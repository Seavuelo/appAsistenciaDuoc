import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EscogerasignaturasPage } from './escogerasignaturas.page';

describe('EscogerasignaturasPage', () => {
  let component: EscogerasignaturasPage;
  let fixture: ComponentFixture<EscogerasignaturasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(EscogerasignaturasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
