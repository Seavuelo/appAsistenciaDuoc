import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DetallesclaseprofePage } from './detallesclaseprofe.page';

describe('DetallesclaseprofePage', () => {
  let component: DetallesclaseprofePage;
  let fixture: ComponentFixture<DetallesclaseprofePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DetallesclaseprofePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
