import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CrearasignaturaprofePage } from './crearasignaturaprofe.page';

describe('CrearasignaturaprofePage', () => {
  let component: CrearasignaturaprofePage;
  let fixture: ComponentFixture<CrearasignaturaprofePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CrearasignaturaprofePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
